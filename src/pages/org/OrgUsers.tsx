import React from 'react';
import { ComingSoonOverlay } from '../../components';

const OrgUsers: React.FC = () => {
  return (
    <ComingSoonOverlay 
      title="Organization Users Management"
      description="Organization user management features are currently being developed."
      phase="Phase 2"
    />
  );
};

export default OrgUsers; 