import React from 'react';
import ComingSoonOverlay from '../../components/ComingSoonOverlay';

const NFCSettings: React.FC = () => {
  return (
    <ComingSoonOverlay 
      title="NFC Settings"
      description="Configure NFC card settings and custom URLs"
      phase="Q1 2024"
    />
  );
};

export default NFCSettings; 